# Root environment configuration for local MCP servers
# Copy to .env and fill in your actual credentials.
# IMPORTANT: Never commit the .env file - it's in .gitignore for security

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================

# PostgreSQL connection string for the MCP Postgres server
# Used by: MCP server for database operations, local development
# Format: postgresql://username:password@host:port/database
# Setup:
#   1. Install PostgreSQL 17: https://www.postgresql.org/download/windows/
#   2. Set password for 'postgres' role: ALTER USER postgres WITH PASSWORD 'your_password';
#   3. Create database: CREATE DATABASE theporadas_dev;
#   4. Replace CHANGE_ME below with your actual password
PG_URL=postgresql://postgres:CHANGE_ME@localhost:5432/theporadas_dev

# ============================================================================
# MONITORING & ERROR TRACKING (Optional)
# ============================================================================

# Sentry integration has been removed/disabled for local development.
# If you need Sentry for production deployment, uncomment and configure:
# SENTRY_AUTH_TOKEN=sntryu_your_token_here
# SENTRY_DSN=https://publicKey@o12345.ingest.sentry.io/12345

# ============================================================================
# AI & AUTOMATION (Optional)
# ============================================================================

# OpenAI API Key - Used for AI-powered features
# Get your key: https://platform.openai.com/api-keys
# Used by: Sentry MCP analysis features, optional AI integrations
OPENAI_API_KEY=sk-your-openai-key

# ============================================================================
# VERSION CONTROL & SEARCH (Optional)
# ============================================================================

# GitHub Personal Access Token - Enables GitHub MCP server features
# Create token: https://github.com/settings/tokens
# Required scopes: repo, read:org, read:user
# Used by: GitHub MCP server for repo operations, PR analysis, issue management
GITHUB_PERSONAL_ACCESS_TOKEN=ghp_yourtoken

# Brave Search API Key - Enables web search in Copilot
# Get your key: https://brave.com/search/api/
# Rate limit: 1 request per second (free tier)
# Used by: Brave Search MCP server for documentation lookup, research
BRAVE_API_KEY=your_brave_api_key

# ============================================================================
# FIREBASE DEPLOYMENT (Optional)
# ============================================================================

# Firebase CI Token - For non-interactive deployments (CI/CD pipelines)
# Generate token: firebase login:ci
# Used by: GitHub Actions, automated deployment scripts
# Note: For local development, use 'firebase login' instead
# FIREBASE_TOKEN=your_firebase_ci_token
