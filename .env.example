# Root environment configuration for local MCP servers
# Copy to .env and fill in your actual credentials.

# PostgreSQL connection string for the MCP Postgres server.
# Update the password portion after you set a local password for the "postgres" role.
PG_URL=postgresql://postgres:CHANGE_ME@localhost:5432/theporadas_dev
